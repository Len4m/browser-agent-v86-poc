{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Len4m/browser-agent-v86-poc/schemas/vm-profile.schema.json",
  "title": "Browser Agent v86 VM profile",
  "description": "Schema for managed VM profiles. Every profile uses an immutable root HDA plus an optional in-memory or IndexedDB OverlayFS workspace.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "name",
    "description",
    "alpineVersion",
    "minimumRamMb",
    "minimumVramMb",
    "storage",
    "packages",
    "allowedTools"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional editor hint pointing to this schema."
    },
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9][a-z0-9.-]*$",
      "description": "Stable profile identifier. Used as manifest filename, selector value, build id prefix and generated initramfs filename."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable profile name shown in the UI."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Short profile summary shown in the UI and generated manifests."
    },
    "alpineVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+(\\.[0-9]+)?$",
      "description": "Exact Alpine minirootfs version used as the rootfs base, for example 3.23.4."
    },
    "alpineBranch": {
      "type": "string",
      "pattern": "^v[0-9]+\\.[0-9]+$",
      "description": "Optional APK/Docker branch override. Defaults from alpineVersion, for example 3.23.4 -> v3.23."
    },
    "kernelOutput": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(public/)?v86/images/.+",
      "description": "Optional kernel path override. Default derives from alpineVersion, for example 3.23.4 -> v86/images/kernels/alpine-v3.23-vmlinuz-lts."
    },
    "minimumRamMb": {
      "type": "integer",
      "minimum": 64,
      "description": "Minimum guest RAM required by this profile in MB. The UI disables lower values and runtime validation rejects them."
    },
    "minimumVramMb": {
      "type": "integer",
      "minimum": 0,
      "description": "Minimum VGA memory required by this profile in MB. The UI disables lower values and runtime validation rejects them."
    },
    "storage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["layout", "rootDiskMb", "workspaceDiskMb", "blockSize", "filesystem"],
      "properties": {
        "layout": {
          "type": "string",
          "const": "overlay-hda",
          "description": "Managed profiles boot a minimal initramfs and switch to immutable HDA plus an optional OverlayFS workspace."
        },
        "rootDiskMb": {
          "type": "integer",
          "minimum": 128,
          "description": "Logical size of the immutable ext4 root disk. Required for overlay-hda."
        },
        "workspaceDiskMb": {
          "type": "integer",
          "minimum": 128,
          "description": "Logical size of the ext4 OverlayFS disk, backed by memory or IndexedDB according to the user's choice."
        },
        "blockSize": {
          "type": "integer",
          "enum": [65536],
          "default": 65536,
          "description": "Logical block size used by the browser CoW disk."
        },
        "filesystem": {
          "type": "string",
          "enum": ["ext4"],
          "default": "ext4"
        }
      }
    },
    "packages": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "contains": { "const": "python3" },
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^[A-Za-z0-9_.:+-]+$"
      },
      "description": "Alpine packages installed into the exported rootfs before packing the initramfs. Must include python3 because the guest serial/tool runners depend on it."
    },
    "allowedTools": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^[A-Za-z0-9_.-]+$"
      },
      "description": "Ordered LLM tool allowlist for this VM profile. Order is used as default priority when models see a limited number of tools."
    },
    "extraRepositories": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^https?://.+"
      },
      "description": "Extra APK repositories appended after the main/community repositories."
    },
    "firstBootCommands": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Shell commands written to /etc/browser-agent-firstboot.sh and executed during VM boot."
    },
    "buildCommands": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Shell commands executed by scripts/setup/vm-alpine-overlay-hda.sh against /rootfs during image generation."
    },
    "bootMessage": {
      "type": "string",
      "minLength": 1,
      "description": "Message displayed on serial0 after first boot setup."
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Maintainer notes for profile-specific package/repository decisions."
    }
  }
}
